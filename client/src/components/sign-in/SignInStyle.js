import { createUseStyles } from 'react-jss';

export default createUseStyles((theme) => ({
  root: {
    maxWidth: '600px',
  },
  rootWrapper: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
}));
